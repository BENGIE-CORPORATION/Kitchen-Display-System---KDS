apiVersion: 1

datasources:
  - name: Prometheus
    uid:  prometheus               # UID fijo requerido por las reglas de alertas
    type: prometheus
    access: proxy
    url: http://prometheus:9090   # nombre del servicio en docker-compose
    isDefault: true
    editable: true
    jsonData:
      timeInterval: "15s"
