# ── Secretos — nunca deben entrar al repo ────────────────────
.env
Backend/.env
Backend/.env.production
Backend/.env.development

# ── macOS ─────────────────────────────────────────────────────
.DS_Store

# ── Docker ────────────────────────────────────────────────────
# Los archivos de override locales (si los usas)
docker-compose.override.yml
